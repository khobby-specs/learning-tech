
    1. Function: it’s a command that tell the interpreter what to do. Eg. print, input etc
    2. Argument: its an input to a fuction that somehow influences its behaviour. They could be integers, letters and string and more.
    3. Side effects: side effect are the aftermath or results seen after the intepreter reads the function and its arguments. They could be  text, values and images.
    4. Return values: are values a function gives back or returns to us after running
    5. Variables: are container for data values in computer memory. They mostly point to an address in the computer’s memory. Unlike c where assigning a new value or variable name will point to a new address, in python, the old variable and the new one point to the same address 
    6. variable name: is a symbolic reference say X that point to a specific object stored in the computer memory
    7. Comments: are notes to yourself in your code. They Are included by way of special character. In python, it is #
    8. Strings: are sequence of characters passed through a function in quotes.
    9. Parameters: are sets of brackets in python which strings and arguments are passed through
    10. 

argument is an input to a function that somehow influences its behaviour.
side effect results seen from after the interpreter reads the function and arguments. it could be text, visual or image.
bugs are mistakes in a program. they serve as problem for you to solve.
Return values are the value a function gives after running
variables are container for some value
comments are notes to yourelf in your code. They include by way of special symbol. In python, its the # symbol. They can also serve as a to do list.
pseudocode
str(string)

